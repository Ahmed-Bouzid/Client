/**
 * Components - Index Global
 * 
 * Export central de tous les composants du nouveau design system.
 * NOTE: 9 composants UI primitives (Card, Input, Badge, Background, BottomSheet,
 * FoodCard, CategoryPill, CartItem, BottomNav) archivés en
 * _archive_2026-05-12_phase0/components/ (audit 0.2.4 — orphelins runtime).
 * Ne pas réintroduire sans décision Phase 1.
 */

// UI Components
export { Button } from './ui/Button';

